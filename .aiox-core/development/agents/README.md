# Agentes locais do MissionAgent

Esta pasta guarda os ficheiros `.md` dos agentes para ambiente autocontido.

Estrutura esperada:

- `.aiox-core/development/agents/*.md`

Podes criar novos agentes pelo próprio app (sidebar -> `+ Novo`) quando `MISSION_AGENT_EDIT` estiver ativo.
